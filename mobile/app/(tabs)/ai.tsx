import { View, Text } from "react-native";
export default function AIScreen() {
  return (
    <View style={{ flex:1, alignItems:"center", justifyContent:"center" }}>
      <Text style={{ fontSize:26 }}>🤖 AI</Text>
      <Text>Chat / planner will live here</Text>
    </View>
  );
}
